unit vid;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, StdCtrls;

type
  Tfvid = class(TForm)
    Button1: TButton;
    procedure Button1Click(Sender: TObject);
    procedure FormClose(Sender: TObject; var Action: TCloseAction);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  fvid: Tfvid;

implementation

{$R *.dfm}

procedure Tfvid.Button1Click(Sender: TObject);
begin
Close;
end;

procedure Tfvid.FormClose(Sender: TObject; var Action: TCloseAction);
begin
 Action:=CaFree
end;

end.
